name: Deploy

on:
  push:
    branches: [main, master]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image
        run: docker build -t pdf-chat-backend:${{ github.sha }} .

      - name: Build summary
        run: echo "Image pdf-chat-backend:${{ github.sha }} built successfully"
